# Este directorio almacena los logs del bot

Los archivos de log se generarán automáticamente cuando configures:

```env
LOG_FILE=logs/trading_bot.log
```

**Nota:** Los archivos `.log` están excluidos del control de versiones (ver `.gitignore`)
