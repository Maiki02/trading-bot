# =============================================================================
# TradingView Pattern Monitor - Dependencies
# =============================================================================
# MVP v0.0.1 - Python 3.10+
#
# Core Dependencies for Real-Time Trading Analysis System
#
# Installation:
#   pip install -r requirements.txt
#
# =============================================================================

# WebSocket Communication
websockets==12.0
# Async WebSocket client for TradingView data streaming

# IQ Option API
# NOTE: iqoptionapi conflicts with pyquotex (websocket-client version clash).
# Install separately ONLY when using DATA_PROVIDER=IQOPTION:
#   pip install git+https://github.com/iqoptionapi/iqoptionapi.git
# git+https://github.com/iqoptionapi/iqoptionapi.git

# HTTP Client (Async)
aiohttp==3.11.16
# Async HTTP client for Telegram API requests
# 3.11.x has prebuilt wheels for Python 3.13 on Windows

# Data Processing & Analysis
pandas==2.2.3
# DataFrame-based time series management and vectorized calculations
# 2.2.x has prebuilt wheels for Python 3.12 on Windows (no compiler needed)

numpy==2.2.4
# Numerical operations for EMA calculations and pattern detection
# 2.x has prebuilt wheels for Python 3.12 on Windows

mplfinance==0.12.10b0
# Candlestick chart generation for pattern visualization

# Configuration Management
python-dotenv==1.0.0
# Environment variable management from .env files

# Quotex API
pyquotex @ git+https://github.com/cleitonleonel/pyquotex.git
# Quotex API client for real-time market data
# Note: pip package name is "pyquotex", not "quotexpy"

# Required for QUOTEX_AUTH_METHOD=CREDENTIALS (browser login flow)
# Install inside the venv:
#   python -m pip install playwright
#   python -m playwright install chromium

# Optional: Performance & Monitoring
# colorama==0.4.6
# Cross-platform colored terminal output (already handled by ANSI codes)
